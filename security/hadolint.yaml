# Hadolint configuration tuned for the course service
ignored:
  # Allow using slim Python base without pinning OS packages via apt
  - DL3008
  # Allow pip installs without explicit hashes (handled via requirements)
  - DL3042
failure-threshold: warning
